import java.util.*;

public class UnoPlayer
{
    private ArrayList<UnoCard> hand = new ArrayList<>();

    public UnoPlayer()
    {
        this.hand = hand;
    }

    /*public void deal(UnoDeck deck)
    {
        if(deck.getSize() == 0)
        {
            deck.reshuffle();
        }
        hand.add(deck.draw());
    }*/
}
